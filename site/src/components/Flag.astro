---
/** Note de relecture inline : surligne un texte à trancher avec le client. */
interface Props { note: string }
const { note } = Astro.props;
---
<span class="flag" data-note={note} tabindex="0"><slot /></span>
